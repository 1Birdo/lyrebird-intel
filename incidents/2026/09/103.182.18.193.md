# Incident: 103.182.18.193

| | |
|---|---|
| Source | `103.182.18.193` |
| Country | Vietnam |
| First seen | 2026-09-14 20:35:27 UTC |
| Last seen | 2026-09-14 20:35:53 UTC |
| Sessions | 1 |
| Events | 50 |
| Services abused | ssh |
| Ports | 22 |

## Credentials attempted

1 attempts across 1 distinct pairs.

| username | password | count |
|---|---|---|
| `root` | `Admin@123` | 1 |

## Commands executed

Verbatim attacker input. Lyrebird's responses are deliberately not published.

```
pwd
whoami
ssh -V
env \| head -10
uname -a
ls -la /
ps aux \| head -10
netstat -tulpn \| head -10
history \| tail -5
uptime
mount \| head -5
hostname
netstat -tulpn 2>/dev/null \| grep LISTEN \| head -20
nproc 2>/dev/null \|\| grep -c ^processor /proc/cpuinfo
grep 'model name' /proc/cpuinfo 2>/dev/null \| head -1 \| cut -d: -f2
top -bn1 2>/dev/null \| grep -i '^%Cpu\\|^Cpu' \| head -1
cat /proc/loadavg 2>/dev/null
free -h 2>/dev/null \| grep -i '^Mem:'
free 2>/dev/null \| grep -i '^Mem:' \| awk '{printf "%.1f", ($3/$2)*100}'
df -h / 2>/dev/null \| tail -1
lspci 2>/dev/null \| grep -iE 'vga\|3d\|display'
lshw -C display 2>/dev/null \| grep -E 'product\|vendor' \| head -4
uptime -p 2>/dev/null \|\| uptime
ls -la /etc/network/interfaces /etc/sysconfig/network-scripts/ /etc/netplan/ 2>/dev/null \| head -5
ip addr show 2>/dev/null \| grep -E '^[0-9]+:' \| head -5
ip route show 2>/dev/null \| head -3
echo 'test' > /tmp/test_1789418142
rm -f /tmp/test_1789418142
cat /etc/passwd 2>/dev/null \| head -1
cat /etc/shadow 2>/dev/null \| head -1
cat /proc/version 2>/dev/null \| head -1
id
cat /proc/cpuinfo \| grep 'model name' \| head -1
uname -r
which apt
which yum
which pacman
which zypper
systemctl list-units --type=service --state=running 2>/dev/null \| head -10
ping -c 1 8.8.8.8 2>/dev/null \| grep '1 packets transmitted'
```

## Detections

- Host Reconnaissance Commands (11)

---

Collected by a Lyrebird honeypot operated by [birdo.uk](https://birdo.uk). Generated 2026-09-15 18:03 UTC.

Services are emulated; no real host was compromised. Attacker input is reproduced verbatim and is hostile by definition — do not execute anything recorded here.
