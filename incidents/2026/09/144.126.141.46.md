# Incident: 144.126.141.46

| | |
|---|---|
| Source | `144.126.141.46` |
| Country | United States of America |
| First seen | 2026-09-26 10:47:13 UTC |
| Last seen | 2026-09-26 11:00:24 UTC |
| Sessions | 4 |
| Events | 29 |
| Services abused | adb |
| Ports | 5555 |
| Classification | scanner |

## Commands executed

Verbatim attacker input. Lyrebird's responses are deliberately not published.

```
getprop ro.build.version.release
getprop ro.build.version.sdk
getprop ro.product.model
uname -a
which apt 2>/dev/null \|\| command -v apt
which pkg 2>/dev/null \|\| command -v pkg
which apt-get 2>/dev/null \|\| command -v apt-get
which yum 2>/dev/null \|\| command -v yum
which dnf 2>/dev/null \|\| command -v dnf
which pacman 2>/dev/null \|\| command -v pacman
which opkg 2>/dev/null \|\| command -v opkg
which termux-info 2>/dev/null \|\| command -v termux-info
service call iphonesubinfo 1
dumpsys telephony.registry
getprop gsm.sim.state
getprop gsm.operator.alpha
getprop gsm.operator.numeric
```

---

Collected by a Lyrebird honeypot operated by [birdo.uk](https://birdo.uk). Generated 2026-09-26 12:06 UTC.

Services are emulated; no real host was compromised. Attacker input is reproduced verbatim and is hostile by definition — do not execute anything recorded here.
