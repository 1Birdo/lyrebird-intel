# Incident: 85.9.192.124

| | |
|---|---|
| Source | `85.9.192.124` |
| Country | United States of America |
| First seen | 2026-09-16 18:41:21 UTC |
| Last seen | 2026-09-16 18:41:27 UTC |
| Sessions | 3 |
| Events | 35 |
| Services abused | ssh |
| Ports | 22 |
| Classification | malware loader |

## Credentials attempted

3 attempts across 1 distinct pairs.

| username | password | count |
|---|---|---|
| `root` | `@Administrator` | 3 |

## Commands executed

Verbatim attacker input. Lyrebird's responses are deliberately not published.

```
uname -a
which curl \|\| apt install curl -y >/dev/null 2>&1
which lscpu \|\| apt install lscpu -y >/dev/null 2>&1
which free \|\| apt install procps -y >/dev/null 2>&1
which df \|\| apt install coreutils -y >/dev/null 2>&1
hostname
uname -m
cat /etc/os-release \| grep PRETTY_NAME \| cut -d '"' -f2
uname -r
free -h \| grep Mem \| awk '{print $2}'
lscpu \| grep 'Model name' \| cut -d ':' -f2
nproc
df -h / \| awk 'NR==2 {print $2}'
df -h / \| awk 'NR==2 {print $4}'
uptime -p
ps -e --no-headers \| wc -l
who \| wc -l
curl -s ipinfo.io/json
```

## Payload URLs

Addresses this host tried to fetch executable content from.

| url | attempts |
|---|---|
| `ipinfo[.]io/json` | 1 |

## Detections

- Host Reconnaissance Commands (5)

---

Collected by a Lyrebird honeypot operated by [birdo.uk](https://birdo.uk). Generated 2026-09-23 00:11 UTC.

Services are emulated; no real host was compromised. Attacker input is reproduced verbatim and is hostile by definition — do not execute anything recorded here.
