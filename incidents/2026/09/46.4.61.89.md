# Incident: 46.4.61.89

| | |
|---|---|
| Source | `46.4.61.89` |
| Country | Germany |
| First seen | 2026-09-14 01:01:24 UTC |
| Last seen | 2026-09-14 01:21:26 UTC |
| Sessions | 4 |
| Events | 164 |
| Services abused | ssh |
| Ports | 22 |

## Credentials attempted

4 attempts across 4 distinct pairs.

| username | password | count |
|---|---|---|
| `root` | `Prueba123` | 1 |
| `root` | `Munich@123` | 1 |
| `root` | `Frankfurt@1234` | 1 |
| `root` | `123QWEqwe` | 1 |

## Commands executed

Verbatim attacker input. Lyrebird's responses are deliberately not published.

```
whoami
pwd
history \| tail -5
ssh -V
mount \| head -5
ls -la /
netstat -tulpn \| head -10
uptime
env \| head -10
hostname
uname -a
ps aux \| head -10
netstat -tulpn 2>/dev/null \| grep LISTEN \| head -20
ls -la /etc/network/interfaces /etc/sysconfig/network-scripts/ /etc/netplan/ 2>/dev/null \| head -5
ip addr show 2>/dev/null \| grep -E '^[0-9]+:' \| head -5
ip route show 2>/dev/null \| head -3
cat /etc/passwd 2>/dev/null \| head -1
cat /etc/shadow 2>/dev/null \| head -1
cat /proc/version 2>/dev/null \| head -1
id
cat /proc/cpuinfo \| grep 'model name' \| head -1
uname -r
which apt
which yum
which pacman
which zypper
systemctl list-units --type=service --state=running 2>/dev/null \| head -10
ping -c 1 8.8.8.8 2>/dev/null \| grep '1 packets transmitted'
time dd if=/dev/zero of=/tmp/test bs=1M count=10 2>&1
rm -f /tmp/test
ss -tuln 2>/dev/null \| wc -l
last
echo 'test' > /tmp/test_1789347686
rm -f /tmp/test_1789347686
echo 'test' > /tmp/test_1789348027
rm -f /tmp/test_1789348027
echo 'test' > /tmp/test_1789348371
rm -f /tmp/test_1789348371
echo 'test' > /tmp/test_1789348885
rm -f /tmp/test_1789348885
```

## Detections

- Host Reconnaissance Commands (44)

---

Collected by a Lyrebird honeypot operated by [birdo.uk](https://birdo.uk). Generated 2026-09-22 18:06 UTC.

Services are emulated; no real host was compromised. Attacker input is reproduced verbatim and is hostile by definition — do not execute anything recorded here.
